What programming language and package manager does this project use? Look at the project files (package.json, requirements.txt, Gemfile, go.mod, etc).

**IMPORTANT: NEVER read .env, .env.local, or any environment/secret files.**

Output a JSON block:
```json
{ "language": "javascript", "packageManager": "npm", "framework": "express" }
```
